themedir =  $(datadir)/icons/HighContrast-SVG
size = scalable
context = actions

icondir = $(themedir)/$(size)/$(context)

icon_DATA = \
	bookmark-view.svg	\
	history-view.svg

EXTRA_DIST = \
	$(icon_DATA)